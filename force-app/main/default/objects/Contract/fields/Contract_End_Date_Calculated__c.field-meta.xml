<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contract_End_Date_Calculated__c</fullName>
    <description>This formula Calculate the End of the contract using the SM_Plan_Months__c, where if the plan months is bigger of 60 then the End of the contract is TODAY() else Add the Plan Months to Start Date OR SM_Start_date__c using the bigger date</description>
    <formula>IF(NOT(ISNULL(SM_Plan_Months__c))
  ,IF(SM_Plan_Months__c&lt;=60
     ,ADDMONTHS(Contract_Calculated_RC_Start_Date__c,SM_Plan_Months__c)     
     ,IF(ADDMONTHS(Contract_Calculated_RC_Start_Date__c,6)&lt;TODAY()
        ,TODAY()
        ,ADDMONTHS(Contract_Calculated_RC_Start_Date__c,6)
      )
   )  
  ,ADDMONTHS(Contract_Calculated_Start_Date__c,7)  
 )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Contract End Date Calculated</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
