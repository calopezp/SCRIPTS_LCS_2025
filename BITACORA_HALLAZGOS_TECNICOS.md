# Bitácora de hallazgos técnicos — SCRIPTS_LCS_2025

## Cómo usar este archivo

Registro acumulativo de bugs/errores reales encontrados en MONEE (o PREPROD) a lo largo de las
sesiones — no de decisiones de negocio ni de tareas operativas del día a día (esas van en
`CLAUDE.md` o en los handoffs de `COLLECTIONS/`). Pensado para consulta rápida y copiar/pegar
directo a un reporte gerencial cuando haga falta explicar un hallazgo técnico con fecha,
consecuencia, solución y estado actual.

**Se sigue llenando en cada sesión donde aparezca un hallazgo técnico nuevo** (bug real, no un
ajuste de datos puntual) — agregar una fila nueva a la tabla, más abajo el detalle si hace falta
contexto extra. No borrar filas viejas aunque ya estén resueltas — es historial.

---

## Tabla resumen

| Fecha | FIX / ERROR | Consecuencias | Solución | Estado |
|---|---|---|---|---|
| 2026-09-22 | Corrección de 15 payments (validación fresca de todo el universo ACH 2026, 2,620 payments `REJECTED`/`ACH TRANSMITTED` -- no solo el subset de Cancelled/cola de cancelación de los fixes anteriores): `PENDING`/`RETURN` con más de 26 días desde la transmisión, sin ningún reporte de Check Collection que confirme `COLLECTED` pendiente de aplicar. Incluye `PY-01849469`, encontrado porque el pipeline diario automático lo tocó a media sesión (después del snapshot original de la mañana) y por eso no cayó en ningún lote anterior | 7 de los 15 tienen una Late Payment Fee asociada (6 `Stopped` sin activar nunca, $840 total; 1 ya `Completed`) -- verificado que no se toca ninguna orden con este fix, solo el payment original | `TEMP/fix_15_objetivo1_2026.apex`, DRY_RUN validado (15/15 sin saltos) antes del real, sin triggers. Validación 2 objetivos separados: 297 `ACH TRANSMITTED` también revisados contra el timeout de 15 días -- **0 candidatos**, ninguno pasa de 14 días (el pipeline diario ya está al día) | **15/15 corregidos y verificados en vivo** (`REJECTED`/`NOT_COLLECTED`). El universo completo de 2,620 payments ACH 2026 queda validado -- sin más candidatos pendientes bajo estos 2 criterios |
| 2026-09-22 | 4 de los 12 payments de Tier 3 (genuinamente contradictorios, `ACCEPTED`/`COLLECTED` vs. un reporte real que dice `REJECTED`/`NOT_COLLECTED`) resueltos — instrucción explícita del usuario: en contratos ya `Cancelled` (`00315208`), aplicar directamente la determinación del reporte sin más análisis, ya que no afecta cobranza activa en este momento | `PY-01776683/756/774`, `PY-01782501` — sin este criterio hubieran quedado indefinidamente en Tier 3 esperando una decisión que, para un contrato ya cerrado, no tiene urgencia real | `TEMP/fix_t3_cancelled_00315208.apex`, DRY_RUN validado (4/4 sin saltos) antes del real, sin triggers | **4/4 corregidos y verificados en vivo** (`REJECTED`/`NOT_COLLECTED`). Quedan 8 de los 12 T3 originales, todos en contratos activos — sin este mismo criterio de "no urgente" aplicable |
| 2026-09-22 | **Barrido puntual de contratos Cancelled/en cola de Cancelar Contratos**, pedido explícito del usuario: (1) re-aplicar los 286 payments (T5+T8) que quedaron pendientes por contrato recién `Cancelled`; (2) barrido nuevo (fuera del universo original de 3,875) de payments `REJECTED`/`PENDING`\|`RETURN` en contratos `Cancelled` o en `Contratos_para_Cancelar_LOG.csv`, sin importar cuántos días llevan transmitidos, aplicando `NOT_COLLECTED` de una vez si el código de retorno es "definitivo" (**se corrigió la lista documentada en `CLAUDE.md` Rule 3 -- faltaba R10, que sí es definitivo** junto a R02/R04/R07/R13/R16, per `SM_ReturnCodeNotifier`) | El barrido encontró 54 payments distintos en ese universo ampliado: 32 calificaban para corrección inmediata (23 ya con +26 días reales -- nunca se habían revisado antes, incl. 10 payments del mismo contrato `00308911` y 6 del `00314950` -- y 9 más por código definitivo R02/R10 sin importar antigüedad). Los otros 22 (código R01/R08/R09, ≤26 días) se dejaron esperando el plazo normal a propósito | 286 re-aplicados sin problema (contrato ya Cancelled, chequeo de seguridad correctamente les permite el paso). 32 nuevos corregidos a `NOT_COLLECTED` (`TEMP/fix_286_now_cancelled_chunk0-2.apex`, `TEMP/fix_sweep_cancelled_queue_2026.apex`), DRY_RUN validado antes de cada uno, sin triggers | **286/286 + 32/32 corregidos y verificados en vivo**. Los 22 restantes (R01/R08/R09, ≤26 días) quedan documentados para revisar cuando cumplan el plazo |
| 2026-09-22 | **Lote final de la validación de payments 2026**: aplicados los 4 bloques pendientes que el usuario autorizó completos -- 15 T1 (inconsistencia detectada la sesión anterior: ya doblemente validados pero nunca aplicados), 509 T2 (Rule 3, excluyendo los 9 del "Grupo A" que quedan para revisión aparte), 974 T5 (solo formato/casing) y 58 T8 (sub-estado menor). 1,556 payments en total, divididos en 16 lotes de 100 por el límite de tamaño de Anonymous Apex | Al ejecutar, 300 de los 1,556 (luego confirmado 286 tras verificación en vivo) salieron `SALTADO (contrato ya Cancelled)` -- verificado que es real (spot-check confirmó contratos que pasaron a `Cancelled` durante el día, probablemente por la corrida diaria del pipeline o por los mismos desbloqueos de la cola de Cancelar Contratos de hoy), no un bug | Los 16 lotes corridos con DRY_RUN primero, luego real, sin triggers. Verificación final por comparación directa contra Salesforce en vivo (no solo el conteo del debug log) para distinguir aplicados-ahora de ya-coincidían-antes de ya-Cancelled-y-saltados | **1,270 payments confirmados con el valor correcto en vivo** (de los cuales 945 se corrigieron en este lote, el resto ya coincidía). **286 quedan pendientes** -- su contrato pasó a `Cancelled` durante el día; candidatos directos a una pasada del mismo tipo que el fix de los 800 Cancelled de esta mañana |
| 2026-09-22 | **Confirmación con `SM_Payment__History` (auditoría real de campo, no solo el tag de texto) de que el catch-up del 2026-09-09 revirtió pagos genuinamente cobrados de vuelta a `PENDING`.** Sospecha del usuario tras revisar los pendientes: pidió validar si el cambio real fue `ACCEPTED`→`REJECTED` y ahora se estaba corrigiendo de vuelta a `ACCEPTED`. Se consultó `SM_Payment__History` (Field='Payment_Status__c') para los 539 payments 2026 aún pendientes: **36 muestran una transición `ACCEPTED`→`REJECTED` en su historia**. De esos, **6 son reversión falsa confirmada** (cobrados vía reporte real en ene-abr/2026, revertidos sin causa el 2026-09-09) — no estaban en el Tier 1 original porque el reporte bancario correspondiente no está en el índice local (`collections_index.csv`/`returns_index.csv`), un hueco del índice, no de los datos reales de Salesforce. Los otros **15 NO se tocaron**: su último valor real antes de la reversión ya era `NOT_COLLECTED` (7 casos) o el `ACCEPTED` revertido venía del timeout de 15 días sin reporte real, no de un cobro confirmado (4 casos, incl. `PY-01888808/831` ya documentados el 2026-09-18) | Los 6 confirmados (`PY-01779105/00311264`, `PY-01778054/00309301`, `PY-01776680/00315429`, `PY-01776759/00315676`, `PY-01778067/00312779`, `PY-01804492/00311449`) estaban `REJECTED`/`PENDING` en contratos `Activated` pese a haberse cobrado de verdad meses antes -- dato incorrecto de cara a cualquier reporte financiero, y bloqueando indebidamente si alguno entra a un flujo de cancelación | Restaurados a `ACCEPTED`/`COLLECTED` (`TEMP/fix_false_reversion_6_2026.apex`), DRY_RUN validado (6/6 sin saltos) antes del real, sin triggers. **Hallazgo abierto, no resuelto del todo:** solo se revisaron los 539 payments 2026 todavía pendientes de este análisis puntual -- el verdadero alcance del catch-up del 09-09 (cuántos payments más, de cualquier año/contrato, sufrieron la misma reversión sin que ningún análisis posterior los haya vuelto a tocar) sigue sin cuantificarse | **6/6 corregidos y verificados en vivo**. **Pendiente real:** decidir si vale la pena un barrido completo de `SM_Payment__History` (no acotado a este análisis puntual) para cuantificar el alcance total de la reversión del 09-09 -- ver `TAREAS_PENDIENTES.md` |
| 2026-09-22 | **Bug real sin corregir en `SM_PaymentHandler.cls`** (encontrado validando el "Grupo D" del análisis de AC): `updateACHOrderInfo()` marca `Completed` una orden de frecuencia `'Once'` (AC) en cuanto se crea el payment (optimista, antes de cualquier confirmación bancaria). `updateRelatedRecordsByPaymentStatusUpdates()`, cuando el payment luego cambia a `REJECTED` (reporte real del banco), **solo** actualiza el estado de la orden si el contrato está `Canceled` o `Finalized` -- no hay ninguna rama para `Payment Process`/`Activated`, así que la orden se queda `Completed` para siempre aunque el payment ya esté confirmado `REJECTED` | 37 contratos `Payment Process` con AC `Completed` a nivel de orden pero `SM_AC_collected__c=false` y sin ninguna Subscription order creada -- el dato de la orden es simplemente incorrecto/obsoleto, no evidencia de cobro real. Riesgo de que cualquier reporte futuro que consulte `SM_ACH_Order__c.SM_Payment_Status__c` para AC en contratos no cerrados llegue a la conclusión equivocada de que sí se cobró | No corregido en el código esta sesión -- se usó `SM_Contract__r.SM_AC_collected__c` (que sí se setea solo por un reporte real vía `PAYMENT_Accumulate_AC_On_Contract`) como señal confiable en su lugar para decidir el estado real de los 37 payments, y se corrigieron esos 37 payments a `NOT_COLLECTED` (junto con el resto del Grupo C, ver fila del fix de 13/2026-09-22) | **No corregido en el código** -- pendiente agregar una rama `when 'REJECTED'` con manejo explícito para `Payment Process`/`Activated` en `updateRelatedRecordsByPaymentStatusUpdates()` (ej. volver la orden a algo como `Stopped`/`Canceled` según corresponda), para que el próximo AC/Fee rechazado en un contrato activo no repita este mismo dato obsoleto |
| 2026-09-22 | Excepción a la Rule 3 (26 días) por código de retorno definitivo: 5 payments Late Payment Fee (los "segundos payments" de 5 de los 8 contratos del fix anterior) corregidos de `PENDING`→`NOT_COLLECTED` **antes** de cumplir los 26 días (llevaban 25) -- instrucción explícita del usuario: si el código de retorno es de los que `SM_ReturnCodeNotifier` ya clasifica como "nunca se resuelven reintentando" (R02 cuenta cerrada, R04 cuenta inválida, R13 routing inválido), proceder sin esperar el plazo | Contratos `00317788`(R04), `00317916`(R02), `00317918`(R04), `00317927`(R02), `00317861`(R13) -- sus Late Payment Fee (`PY-01890514/520/521/522/540`) quedaban bloqueando la cola de "Cancelar Contratos" 2 días más de lo necesario, dado que el código de retorno ya deja claro que no hay reintento posible | Se dejaron a propósito SIN tocar los otros 2 del mismo lote de 7: `PY-01890523` (R08 -- "pago detenido, puede ser puntual", código explícitamente excluido de la categoría definitiva) y `PY-01890524` (R01/NSF -- se resuelve reintentando) -- estos sí esperan el plazo normal de Rule 3, elegibles el 2026-09-24. Validado antes de aplicar: 0 anomalías, ninguno Test/VIP, órdenes ACH ya `Canceled` | **5/5 corregidos y verificados en vivo** — todos `REJECTED`/`NOT_COLLECTED`. Patrón reutilizable: un código de retorno de la lista definitiva (R02/R04/R07/R13/R16) es motivo suficiente para saltarse la espera de 26 días de la Rule 3 |
| 2026-09-22 | Corrección de 13 payments tipo AC (`PENDING`/`RETURN` → `NOT_COLLECTED`, Rule 3) pertenecientes a contratos `Payment Process` que están en el lote activo de "Cancelar Contratos" (`COLLECTIONS/CANCELACIONES/`), bloqueados como `PENDING_ACH_PAYMENT`. Corresponden al "Grupo C" del análisis de AC (orden ACH ya `Canceled`, `SM_AC_collected__c=false`, sin ninguna Subscription order -- sin evidencia de cobro real). Se encontró que varios ya traían una nota histórica manual previa con el patrón `"PENDING>NOT COLLECTED X DIAS (26-41 CAL,)_<payment>_u.<fecha>"` -- confirma que esta misma regla de 26 días ya había sido identificada manualmente antes de esta sesión, solo nunca se aplicó de forma sistemática | Excluido a propósito `PY-01873013`/`00318054` (mismo lote, pero figura `REVISAR_MANUAL` -- `SM_Customer_Cancellation__c=true` + 2 órdenes ACH, una todavía `Initiated` -- no es un simple cambio de estado). Nota: 8 de los 13 contratos tienen OTRO payment (`PY-01890xxx`, probablemente la Late Payment Fee generada por el rechazo) también `PENDING` -- este fix no los desbloquea del todo por sí solo | Validado antes de aplicar: 0 anomalías (ninguno cambió desde el análisis, ninguno Test/VIP, ninguno con contrato ya Cancelled). Confirmado que ningún Flow escucha `SM_Check_Collection_Status__c` y que `Payment_Status__c` no cambia (se queda `REJECTED`), así que ningún Flow/trigger aplica. Anonymous Apex con `SM_TriggerHandler.avoidAllHandlerExcecution`, DRY_RUN validado (13/13 sin saltos) antes del real | **13/13 corregidos y verificados en vivo** — todos ahora `REJECTED`/`NOT_COLLECTED`. Puede desbloquear parcialmente hasta 13 contratos de la cola de cancelación (5 de ellos totalmente, 8 quedan con el segundo payment `PY-01890xxx` pendiente de revisión aparte) |
| 2026-09-22 | Corrección puntual de 2 payments tipo **AC** en contratos `Activated` (`PY-01788739`/00316376, `PY-01788856`/00316539) — pedido explícito del usuario de validar que no se disparara ningún proceso lateral antes de aplicar. Hallazgo real: existe un Flow activo, `PAYMENT_Accumulate_AC_On_Contract`, que escucha exactamente `Payment_Status__c` cambiando a `ACCEPTED` + `SM_Type__c='AC'` en `SM_Payment__c` — **`SM_TriggerHandler.avoidAllHandlerExcecution` NO lo bloquea** (ese flag es solo para triggers Apex del framework `SM_TriggerHandler`, no para Flows nativos) | Sin esta verificación se hubiera asumido (como en todos los fixes anteriores de esta sesión) que el bypass cubría todo, cuando en realidad un Flow sí podía haberse disparado y re-acumulado/re-activado el AC de un contrato que ya lo tenía completo -- riesgo real de doble conteo si el gate del Flow no hubiera protegido correctamente | Se leyó la definición completa del Flow (`force-app/main/default/flows/PAYMENT_Accumulate_AC_On_Contract.flow-meta.xml`): su decisión `If_Contract_Eligible` exige `SM_Contract__r.SM_AC_collected__c = false` -- como ambos contratos ya tenían `SM_AC_collected__c=true` de antes, el Flow se dispara pero cae al Default Outcome sin hacer nada. Se descartaron también los otros 5 Flows que escuchan `SM_Payment__c` (ninguno aplica: Create-only, `Pending_to_Refund__c` distinto, `SM_Type__c` distinto, o son Screen Flows no disparados por DML). Verificado en vivo tras aplicar: `LastModifiedDate` del Contract cambió (rollup/DLRS, benigno) pero `SM_AC_collected__c`/`SM_Charged_AC_Amount__c` idénticos, y 0 `SM_ACH_Order__c` nuevas creadas ese día | **Corregido y verificado sin efectos secundarios** — 2/2 exitosos. Patrón a repetir: antes de cualquier fix futuro que cambie `Payment_Status__c` a `ACCEPTED` en un payment `AC`, revisar `PAYMENT_Accumulate_AC_On_Contract` explícitamente (el bypass de triggers Apex no cubre Flows) |
| 2026-09-22 | Validación de Payments transmitidos por ACH en 2026 (todos los contratos, no solo Cancelled) contra los índices de RETURNS/COLLECTIONS + Rule 3 (26 días). De 3,875 payments validables, 2,695 no coincidían. Se aplicó primero **solo el subset de contratos Cancelled**: 9 `REJECTED`/`PENDING`→`ACCEPTED`/`COLLECTED` (reporte semanal posterior confirma cobro) + 791 `PENDING`/`RETURN`→`REJECTED`/`NOT_COLLECTED` (Rule 3, sin reporte que lo resuelva mejor en >26 días). El script completo (800 registros embebidos en un solo Anonymous Apex) chocó con el límite de tamaño de Salesforce ("Script too large") -- se dividió en 8 lotes de 100 | En contratos `Cancelled` esto es solo exactitud de dato (no hay cobranza activa que afecte) — pero se cruzó contra el lote activo de "Cancelar Contratos" (`COLLECTIONS/CANCELACIONES/`): 65 de los 90 contratos bloqueados como `PENDING_ACH_PAYMENT` tenían el bloqueo únicamente por uno de estos payments obsoletos -- se desbloquean con este fix. También se investigó (a pedido del usuario) si esto reactiva las 307 Late Payment Fee `Stopped` asociadas: confirmado en el código (`SM_LateFeeReconciliationHandler.cls`, comentario del autor) que **no existe automatización para la dirección `NOT_COLLECTED` → reactivar LPF** -- solo existe un script manual con reglas de PTP aparte. Del total $131,766.25 en T2, solo $45,140.85 (34%) corresponde a relaciones todavía activas (Subscription con orden `Initiated`, o LPF-attempt en contrato Activated); $73,818.87 (56%) está atado a órdenes ya `Canceled` | Anonymous Apex dividido en 8 lotes de 100 (`TEMP/fix_cancelled_2026_chunk0-7.apex`), `SM_TriggerHandler.avoidAllHandlerExcecution=true`, DRY_RUN validado (800/800 sin saltos) antes de cada lote real. Quedan sin tocar, a propósito, 17 T1 + 537 T2 en contratos `Activated`/`Payment Process`/`Finalized` ($56,681.27) -- pendientes de que el usuario decida, ver `TAREAS_PENDIENTES.md` | **800/800 corregidos y verificados en vivo** (9 `ACCEPTED`/`COLLECTED`, 791 `REJECTED`/`NOT_COLLECTED`). Reporte completo en `TEMP/validacion_payments_2026_2026-09-22.csv` |
| 2026-09-22 | Continuación del mismo análisis: corrección de los 116 payments restantes (Tier 3 "solo formato" + Tier 4 "sub-estado menor"). Al construir el fix aparecieron 2 hallazgos nuevos: (1) **bug de Apex `==` case-insensitive para Strings** (`'Collected' == 'COLLECTED'` da `true`) -- el chequeo de "ya coincide, no tocar" del script saltaba de más los 6 casos de Tier 3 (justo los que solo necesitaban corregir el casing), corregido con `.equals()` (case-sensitive real) antes de aplicar nada; (2) **18 de los 116 se excluyeron a propósito** (subset `NOT_COLLECTED`/`Not_collected` → `PENDING`) -- su único soporte era la misma fila ambigua de `ResumenOctNovDec2025.pdf` con razón `PASTDUE DATE <fecha futura>`, y 16 de los 18 ya tienen en vivo una fecha posterior al reporte + `SM_Return_code__c='R01'` poblado -- misma señal que en `PY-01738648`/`PY-01741839` (fila anterior) de que un evento real posterior, no indexado, ya tocó el registro. Revertirlos a `PENDING` hubiera repetido el mismo error en dirección contraria | Sin el fix de `.equals()`, 6 registros hubieran quedado sin corregir el casing silenciosamente (el script hubiera reportado "0 saltados por error" pero en realidad los saltaba por el bug). Sin la exclusión de los 18, se hubiera revertido a `PENDING` un sub-estado que probablemente ya es `NOT_COLLECTED` correcto por una vía no indexada | 98/116 corregidos (`TEMP/fix_tier3_tier4_format_cancelled_2025.apex`, DRY_RUN validado 98/98 antes de aplicar, sin triggers): 92 `PENDING`→`NOT_COLLECTED` (reporte semanal posterior confirma resultado final) + 6 solo casing. Los 18 restantes quedan sin tocar, documentados en `TEMP/excluidos_18_not_collected_a_pending_2026-09-22.csv` para revisión aparte (buscar si existe un reporte real de Nov-2025 para esos contratos que explique la fecha/R01 ya presente en vivo) | **98/98 corregidos y verificados en vivo** (2 en `ACCEPTED`/`COLLECTED`, 96 en `REJECTED`/`NOT_COLLECTED`). **18 investigados y cerrados** (ver fila siguiente): Carlos confirmó la regla de los 26 días, que explica por qué ya estaban bien y no hacía falta tocarlos |
| 2026-09-22 | Resolución de los 18 payments excluidos del fix anterior: Carlos aportó la regla de negocio faltante — `SM_Check_Collection_Status__c='PENDING'` con más de 26 días desde `SM_Transmission_Date_ACH_File__c` pasa a `NOT_COLLECTED` (timeout de resolución, nunca implementado en ningún script de este repo, distinto del timeout de 15 días de `mark_transmitted_accepted.apex` que va en la dirección contraria: `ACH TRANSMITTED` sin reporte → `ACCEPTED`) | Sin esta regla, los 18 payments se veían como un desajuste real (`NOT_COLLECTED` en vivo vs. `PENDING` según `ResumenOctNovDec2025.pdf`) que hubiera llevado a revertirlos por error | Verificado con datos reales: los 18 tienen `SM_Transmission_Date_ACH_File__c` de 300+ días atrás — muy por encima del umbral de 26 días — consistente con que su `NOT_COLLECTED` actual es el resultado correcto. Regla documentada en `CLAUDE.md` sección 2.2 (Rule 3, junto a las otras 2 reglas explícitas del usuario) para futuras sesiones | **Cerrado sin necesidad de tocar datos** — los 18 ya estaban correctos. Pendiente (no urgente, preguntar si se quiere): decidir si esta regla se automatiza como un script recurrente, similar a `mark_transmitted_accepted.apex` |
| 2026-09-22 | Validación de Payments en contratos `Cancelled` transmitidos por ACH en 2025 vs. los índices de RETURNS/COLLECTIONS (pedida por Carlos al notar `PY-01762889` en `REJECTED`/`PENDING` pese a un reporte real de Check Collection que ya decía `COLLECTED`). De 4,334 payments con reporte cruzado, 155 no coincidían con el reporte más reciente -- **37 con soporte claro** (reporte semanal posterior, fechado y parseable, confirmaba `COLLECTED` tras un `PENDING` inicial de `ResumenOctNovDec2025.pdf`) quedaron sin corregir porque su fecha de transacción (sep-dic 2025) ya cae fuera de la ventana de 2 meses de la Rule 2 (`CLAUDE.md` 2.2) -- no es un bug nuevo, es la reprocesada pendiente que esa regla exige confirmar explícitamente, nunca disparada para estos 37 en particular. Aparte, **2 payments** (`PY-01738648`, `PY-01741839`) estaban `ACCEPTED`/`Collected` (casing minúsculo, `SM_Historical_Collection_Status__c` NULL -- nunca tocados por el pipeline real) por un mismo lote de mass-update del 2026-08-04 sin ningún reporte bancario real que lo respalde | $3,101.50 en 37 payments mostrando incorrectamente `REJECTED`/`PENDING` en contratos ya Cancelled (dato histórico/contable incorrecto, sin impacto de cobro real por estar Cancelled) + 2 payments marcados `Collected` sin respaldo real | Corrección puntual con `SM_TriggerHandler.avoidAllHandlerExcecution=true` (sin disparar triggers, solo campos de estado), DRY_RUN validado antes de aplicar: 37 → `ACCEPTED`/`COLLECTED` (`TEMP/fix_tier1_collected_cancelled_2025.apex`); 2 → revertidos a `REJECTED`/`PENDING` con la fecha del reporte real, reemplazando la fecha sin respaldo del mass-update (`TEMP/fix_tier2_revert_cancelled_2025.apex`). Quedan **110 payments con solo el sub-estado desactualizado** (`PENDING` vs `NOT_COLLECTED`, mismo `Payment_Status__c` correcto en ambos lados -- sin impacto real) y **6 solo de casing** (`Collected`/`Not_collected` vs picklist en mayúsculas) sin tocar, ver `TEMP/validacion_payments_contratos_cancelados_2025_2026-09-22.csv` | **39/39 corregidos y verificados en vivo** (query posterior confirma 37 en `ACCEPTED`/`COLLECTED` y 2 en `REJECTED`/`PENDING`). Pendiente si se pide: los 110+6 de baja prioridad, y decidir si vale la pena un proceso recurrente de re-confirmación (Rule 2) para que reportes finales tardíos como estos no se queden atascados indefinidamente |
| 2026-09-21 | `SM_ReturnCodeNotifier.sendTimeoutReversalReport()` (correo "[ALERTA] Pagos aceptados por timeout y luego revertidos por reporte real") rediseñado -- pensado originalmente como auditoría técnica interna, ahora es evidencia formal de cuánto se está demorando el banco en reportar un rechazo real (más de los 15/20 días del umbral que dispara la aceptación automática). Objetivo final: mandarlo a Comercial (por ahora sigue solo a Carlos, pendiente la lista real) | El formato anterior mostraba el tag crudo de `SM_Historical_Collection_Status__c` (`PRC_AUT_15D_SIN_REPORTE: ACCEPTED //...`) -- ilegible para alguien fuera del equipo técnico, y no mostraba la fecha de transmisión ACH ni el significado del código de retorno, así que no se podía evidenciar visualmente el atraso del banco | Formato nuevo en texto plano por pago: fecha de transmisión, fecha en que se marcó Aceptado por timeout (calculada como transmisión + días del umbral, ya que el campo que guardaba esa fecha real se sobreescribe cuando llega el reporte que lo revierte), fecha del reporte real, y el código de retorno con su significado (mapa de 13 códigos NACHA comunes). Las 3 fechas en formato `dd mmm yyyy`. Excluye contratos Test/VIP (`== true`, null-safe, mismo patrón que los otros 5 reportes) pero **mantiene** Cancelled/Finalized a propósito -- instrucción explícita del usuario, un reverso en un contrato ya cerrado puede seguir generando costo. Test nuevo verifica el cálculo de fecha y el significado del código | **Desplegado y validado** -- 12/12 tests, verificado con datos reales (7 pagos actuales en el reporte, ej. `PY-01888987`: transmitido 28-ago-2026, aceptado por timeout 12-sep-2026, reportado como PENDING/R11 el 14-sep-2026 -- 17 días de demora real del banco) |
| 2026-09-21 | `SM_ACH_Flow_Monitor.cls` (monitor diario de creación de órdenes ACH para contratos nuevos) tenía 2 bugs reales en su lógica de detección de anomalías: (1) `buildAnomalies()` contaba solo órdenes AC "activas" (`SM_Payment_Status__c NOT IN ('Completed','Canceled')`) para decidir si un contrato "no tiene orden AC" -- pero `Completed` es el estado final EXITOSO de una orden AC (el AC ya se cobró), no algo que deba seguir activo; (2) el universo de contratos a validar usaba `LastModifiedDate >= hace 24h OR CreatedDate >= hace 24h` en vez de solo `CreatedDate` -- el requerimiento real es validar contratos **creados** recientemente, no cualquiera que algo haya tocado | El correo del 2026-09-21 (`[REVISAR] Monitoreo Contratos Activados`) reportó **140 "anomalías"** -- de esas, **136 eran falso positivo** (132 con `SM_AC_collected__c=true` confirmando que sí se había cobrado, ej. `00316097`: orden `ACH-25078` en `Completed`, contrato activo corriendo su subscription normal). Además, el universo de contratos validados incluía **159 contratos "tocados"** en 24h (cualquier LastModifiedDate), cuando el alcance real de contratos recién creados era de solo 4 | (1) Nuevo parámetro `Set<Id> contractsWithAnyAcOrder` en `buildAnomalies()` -- solo marca "sin orden AC" si además de no tener ninguna activa, tampoco tiene ninguna en NINGÚN estado (nueva query sin filtro de status). (2) Filtro de contratos recientes cambiado a `CreatedDate >= :since` únicamente, quitando `LastModifiedDate`. Test nuevo `testBuildAnomalies_completedOrderNotFalsePositive` reproduce el caso real | **Corregido y desplegado** (10/10 tests, 2 deploys). Verificado en vivo: 140→5 anomalías reales con el fix #1 (los 5 restantes son casos genuinos -- 3 contratos migrados 2019-2021, ya cubiertos por el hueco de migración conocido en `CLAUDE.md` 2.3, y 2 no-migrados de 2023/2025 pendientes de investigar si se pide); universo de contratos 159→4 con el fix #2 |
| 2026-09-21 | `SM_Check_Collection_Status__c` (texto libre, no picklist) tiene dos variantes de "no cobrado" circulando: `'NOT_COLLECTED'` (guion bajo, el estándar actual) y `'NOT COLLECTED'` (con espacio, dato legado de 212 `SM_Payment__c` creados jul-2026). `-CancelarContratos.apex`/`-CANCELAR_CONTRATOS_FULL.apex` (`ALLOWED_ACH_STATUSES`) solo reconocían la variante con guion bajo -- 4 scripts distintos (`SM_ReturnCodeNotifier.cls`, `update_check_collection[_isolated].apex`, `update_ach_returns.apex`) ya conocían y manejaban ambas variantes al leer, pero estos 2 no | Al validar 44 contratos del lote de cancelación (documentación/Rosaura), 6 quedaban bloqueados de más por el guardrail de pagos ACH sin resolver -- el guardrail en sí es correcto (nunca cancelar con pagos bancarios pendientes), pero rechazaba contratos que en realidad ya estaban `NOT_COLLECTED` (resuelto) solo por el espacio. Causa raíz confirmada: `extract_check_collection.py` (único script que escribe este campo desde reportes de banco) ya normaliza correctamente al escribir (`SECTION_TO_STATUS`, la clave `"NOT COLLECTED"` es el texto crudo del PDF, el valor escrito a Salesforce siempre es `"NOT_COLLECTED"`) -- los 212 registros con espacio son dato viejo de antes de esa normalización, no una escritura activa | Agregadas las variantes con espacio/casing a `ALLOWED_ACH_STATUSES` en ambos scripts de cancelación (mismo patrón defensivo que ya usaban los otros 4). Además, backfill de datos: los 212 `SM_Payment__c` con `'NOT COLLECTED'` normalizados a `'NOT_COLLECTED'` (`-NORMALIZE_NotCollected_Status.apex`, dry-run validado antes de aplicar; confirmado que `SM_LateFeeReconciliationHandler` -- el único handler activo que lee este campo -- solo reacciona a `'COLLECTED'`, sin riesgo de disparar algo al normalizar) | **Corregido y aplicado** -- 212/212 normalizados, 0 fallidos, verificado con conteo posterior (0 registros con `'NOT COLLECTED'` restantes) |
| 2026-09-21 | Reapareció `COLLECTIONS/mark_transmitted_accepted.apex` (ruta vieja, de antes del move a `UTILITARIOS/` el 2026-09-18) como archivo **sin trackear** en git, con el contenido previo a toda la sesión anterior (`DAYS_SINCE_TRANSMITTED=15` pero `DRY_RUN=false`, el default inseguro ya corregido en `UTILITARIOS/`) -- origen no determinado (candidato más probable: un tab del editor con el buffer viejo que se volvió a guardar solo) | Si alguien hubiera corrido `sf apex run -f COLLECTIONS/mark_transmitted_accepted.apex` por costumbre (la ruta donde vivió el archivo siempre hasta hace 3 días), el UPDATE real se hubiera ejecutado sin ningún preview -- mismo riesgo que el bug de `DRY_RUN=false` ya documentado (fila 2026-09-18), pero reaparecido en la ruta vieja después de haberlo corregido en la nueva | Confirmado con Carlos que no lo creó él a propósito -- eliminado (nunca estuvo trackeado en git, no hay pérdida de historial). Auditoría completa de los 5 `.apex` del pipeline con toggle `DRY_RUN` confirmó que los 5 trackeados quedan en `true` | **Eliminado y auditoría completa**. Recordatorio para el futuro: si alguna vez reaparece un archivo en una ruta que ya se movió deliberadamente, tratarlo como sospechoso -- no asumir que es inofensivo solo porque el contenido se ve "familiar" |
| 2026-09-18 | `mark_transmitted_accepted.apex` tenía `Boolean DRY_RUN = false;` como default **committeado en el repo** -- todos los demás `.apex` del pipeline (`update_check_collection.apex`, `update_ach_returns.apex`) usan `DRY_RUN = true` por default a propósito, porque `run_mark_transmitted_accepted.sh` (su wrapper) solo cambia `true→false` en modo `apply`; en modo preview (sin argumento) no hace nada para forzar `true` -- asume que el archivo ya viene seguro | Correr `./run_mark_transmitted_accepted.sh` (sin `apply`, pensando que era solo preview) en realidad ejecutaba el UPDATE real. Se verificó que esto pasó una vez esta misma sesión (al confirmar que el archivo seguía funcionando tras moverlo a `UTILITARIOS/`) -- por pura casualidad no tuvo efecto porque el catch-up de 20 días de la misma sesión ya había dejado 0 candidatos reales en ese momento (verificado con una query aparte). Con cualquier candidato real pendiente, hubiera aplicado el UPDATE sin que nadie lo pidiera | Cambiado a `Boolean DRY_RUN = true;`, igual que el resto del pipeline. De paso se automatizó como paso 4/4 de `run_daily_new_files.sh` (antes solo manual) usando el mismo patrón seguro que ya usan `run_import_return.sh`/`run_import_collection.sh`: el archivo del repo se copia a un temporal, y SOLO en el temporal (nunca en el archivo trackeado) se hace el swap `true→false` en modo `apply` | **Corregido y verificado** -- dry run completo de `run_daily_new_files.sh` (los 4 pasos) confirmó `MODO: DRY RUN` real para el paso nuevo, sin ningún `UPDATE COMPLETADO` |
| 2026-09-18 | El wildcard `_` de `LIKE` escapado como `\_` (sintaxis correcta vía `sf data query`/API REST) devuelve **0 filas** cuando se usa en SOQL **inline/estático dentro de una clase Apex** -- Apex no procesa el escape igual que `Database.query()` ni la API. Encontrado construyendo `SM_ReturnCodeNotifier.sendTimeoutReversalReport()` (`WHERE SM_Historical_Collection_Status__c LIKE '%D\_SIN\_REPORTE:%'`): la misma cadena de query, probada vía `sf data query -o MONEE` directo, daba el conteo correcto (7 filas); la misma cadena, corrida como SOQL estático dentro de un metodo Apex (`sendTimeoutReversalReport()` ya desplegado) o incluso en Anonymous Apex suelto, daba 0 -- verificado aislando variable por variable (ver detalle abajo) | El nuevo reporte de alerta (ver fila siguiente) salió desplegado y probado en producción sin encontrar los 3 casos reales que sí debía encontrar (`PY-01888808/831/987`) -- el correo decía "Sin pagos" cuando en realidad había 3 pendientes de reportar. Pudo pasar desapercibido si no se hubiera verificado el resultado contra el estado real conocido de esos 3 pagos en la misma sesión | Quitado el escape -- `LIKE '%D_SIN_REPORTE:%'` (wildcard `_` sin escapar) funciona igual de bien aquí porque el texto del tag es fijo (no hay riesgo real de falso positivo por el wildcard siendo más permisivo). Aislado con 4 queries de control vía Anonymous Apex: `LIKE` sin escapar + `AND` simple = 7 filas (correcto); `LIKE` escapado (`\_`) + mismo `AND` simple = 0 filas (roto) -- confirma que el escape en sí es la única variable que rompe el match, no el resto de la cláusula | **Corregido y redesplegado** (`SM_ReturnCodeNotifier.cls`, 12/12 tests) -- verificado en vivo que el correo ya encuentra los 3 casos reales con evidencia completa. Aplica a cualquier SOQL estático futuro que necesite escapar `_` o `%` en un `LIKE` -- usar el wildcard sin escapar cuando el patrón de todas formas no genera ambigüedad real |
| 2026-09-18 | Guard de regresión de `update_check_collection.apex`/`update_ach_returns.apex` (excepción para revertir un `ACCEPTED` que no es cobro confirmado por el banco) solo reconoce el tag `D_SIN_REPORTE:` de `mark_transmitted_accepted.apex` -- cualquier OTRO script que marque `ACCEPTED` sin confirmación real del banco, con un tag de historial distinto, **no** califica para la excepción y queda bloqueado igual que un `ACCEPTED` real cuando llega el reporte verdadero | Caso real: `PY-01888816` (contrato `00317953`, Cancelled) fue marcado `ACCEPTED` por `TEMP/fix_stuck_cancelled_contracts.apex` (tag `PRC_AUT_FIX_CANCELLED_CONTRACT_ORDER:`) el 2026-09-16 -- no por timeout. El 2026-09-18, dos reportes reales (Returns 08-27 y Check Collection 09-15, este último más reciente y ganador por regla de negocio) intentaron corregirlo a `REJECTED`/`PENDING`/R11 y quedaron bloqueados por el guard, mientras sus 3 "hermanos" del mismo lote (`PY-01888808/831/987`, sí tageados `D_SIN_REPORTE:`) se corrigieron solos sin problema en el mismo catch-up de 13 fechas | Corrección manual puntual (`TEMP/fix_py01888816_regression.apex`, DRY_RUN validado antes de aplicar): mismos valores que hubiera aplicado el reporte real (`Payment_Status__c='REJECTED'`, `SM_Check_Collection_Status__c='PENDING'`, `SM_Return_code__c='R11'`, fecha 2026-09-15, tag `PRC_AUT: PENDING //` antepuesto al histórico existente), con `SM_TriggerHandler.avoidAllHandlerExcecution=true` para no re-disparar el intento de `SM_PaymentHandler` de tocar la ACH Order (`ACH-28544`, ya `Completed` por el fix del 2026-09-16 -- verificado que no se movió tras el update) | **Corregido el caso puntual, verificado en vivo**. **No corregido en el código**: el patrón reconocido por la excepción del guard sigue siendo solo `D_SIN_REPORTE:` -- cualquier futuro script ad-hoc que marque `ACCEPTED`/`COLLECTED` sin confirmación bancaria real va a necesitar el mismo tipo de corrección manual cuando llegue el reporte verdadero, salvo que se decida ampliar el patrón reconocido (pendiente, ver `TAREAS_PENDIENTES.md`) |
| 2026-09-17 | `mark_transmitted_accepted.apex` tumbaba varios pagos por corrida con `NullPointerException` en `SM_ACHOrderHandler.insertNextScheduledPaymentDate` (linea ~599) -- causa raiz: el subflow `CONTRACT_Create_ACH_Subscription_Order` toma `Contract.SM_Start_date__c` para `SM_ACH_Order__c.SM_Next_Transaction_Date__c` **sin validar que no sea null** (a diferencia de `CONTRACT_Create_ACH_AC_Order`, que si valida `SM_AC_start_date__c IsNull=false` antes de crear la orden AC) | Contrato real `00318208`: `SM_Requires_RC_Payment__c=true` pero `SM_Start_date__c` en blanco -- al marcarse su AC como cobrado (`SM_AC_collected__c=true`), el orquestador `CONTRACT_10 After Save` disparo la creacion de la Subscription Order con fecha null y tumbo el DML completo de esa oleada. Auditoria completa de MONEE: solo 4 contratos activos en todo el org tenian este hueco (`00316161`, `00318106`, `00318208`, `00318425`) -- 1 de ellos (`00316161`) resulto ser `Is_Test_Contract__c=true` | 3 Validation Rules nuevas en `Contract` (`VR016_RCStartDateRequired`, `VR017_RCPaymentDayRequired`, `VR018_ACStartDateRequired`) que bloquean cualquier cambio de `Status` (saliendo de Draft/Finalized/Cancelled) si falta `SM_Start_date__c`/`SM_Payment_day__c` (cuando `SM_Requires_RC_Payment__c=true`) o `SM_AC_start_date__c` (cuando `SM_Requires_AC_Payment__c=true` -- OJO: no es el mismo gate que RC, hay 92 contratos "solo RC" sin AC que legitimamente no llevan esta fecha). Las 3 reglas excluyen `Contract_Type__c='Dependent'`, `Is_Test_Contract__c`, `Is_VIP_Contract__c`, `SM_Full_Payment__c`, `SM_Customer_Cancellation__c`, `SM_Billing_status__c='PAYMENT COMPLETED'` y `CB_Subscription__c` no-null -- mismo criterio de exclusion que ya usan los 2 Flows de creacion de orden ACH, validado contra datos reales antes de desplegar (sin esto, la primera version de la regla hubiera bloqueado al contrato `00316161` de prueba) | **Desplegado y validado en MONEE** (las 3 VR activas). Los 4 contratos con datos incompletos se dejaron sin tocar a proposito -- Comercial ya los tenia identificados y los estaba corrigiendo en paralelo (3 de los 4 se resolvieron solos durante esta misma sesion) |
| 2026-09-17 | Ningun script diario de `COLLECTIONS/` (`mark_transmitted_accepted.apex`, `update_check_collection[_isolated].apex`, `update_ach_returns.apex`, `update_transmission_date.apex`) ni `SM_ReturnCodeNotifier.cls` (los 5 `notify_*.apex`) filtraba contratos `Is_Test_Contract__c`/`Is_VIP_Contract__c` -- a diferencia de los Flows de creacion de orden ACH, que si los excluyen | Validado en vivo: 0 impacto real hoy (ningun contrato Test/VIP tiene pagos `ACH TRANSMITTED`/`ACH PENDING` en este momento) -- era un hueco real pero todavia teorico | Agregado el filtro (`Is_Test_Contract__c=false AND Is_VIP_Contract__c=false`, o el guard equivalente antes de aplicar) a los 5 scripts `.apex` de `COLLECTIONS/` y a `SM_ReturnCodeNotifier.buildReport()` (con su test nuevo `skipsTestAndVipContracts`) | **Desplegado y validado en MONEE** (`SM_ReturnCodeNotifier`, 9/9 tests). Los scripts `.apex` sueltos son Anonymous Apex (no requieren deploy), ya actualizados en el repo |
| 2026-09-16 | `SM_PaymentHandler.updateRelatedRecordsByPaymentStatusUpdates()` (línea 294) hace `Database.update(achOrdersToUpdate)` **sin** `allOrNone=false` sobre la lista completa de órdenes ACH tocadas en el lote -- si UNA sola orden falla (ej. bloqueada por `SM_ACHOrderHandler.validateCanceledStatus()`, línea 530, que impide pasar una orden a `Canceled` mientras tenga algún pago hermano en `ACH_PENDING`/`ACH_TRANSMITTED`), el DML entero revienta y arrastra como "fallidos" a TODOS los demás pagos del mismo lote, aunque no tengan nada que ver | Al correr `mark_transmitted_accepted.apex` (15 días sin reporte → ACCEPTED), 200 de 227 pagos de la Oleada 1 aparecieron como error -- pero los 200 mensajes de error apuntaban a la MISMA orden (`ACH-28209`, contrato `00316932` ya `Cancelled`, con 2 pagos hermanos atascados en `ACH TRANSMITTED`). Barrido completo del org: exactamente 11 pagos en 10 órdenes distintas, todos en contratos `Cancelled`, eran el 100% de los casos de este tipo. Además se encontró una inconsistencia real en la misma lógica (líneas 264-289): un pago `ACCEPTED` en un contrato `Cancelled` manda la orden a `'Canceled'` -- igual que un `REJECTED` -- cuando debería quedar `'Completed'` (el pago sí se cobró, la orden cumplió su propósito; `'Canceled'` solo tiene sentido si el pago falló) | Backfill puntual (`TEMP/fix_stuck_cancelled_contracts.apex`, DRY_RUN validado antes de aplicar): Paso A, los 11 pagos → `ACCEPTED`/`COLLECTED` con `SM_TriggerHandler.avoidAllHandlerExcecution=true` (evita que la misma transacción dispare el choque); Paso B, sin bypass, solo la orden que de verdad lo necesitaba (`ACH-26647`, `Stopped`→`Completed`) -- las otras 9 ya estaban `Completed` por otra vía y se saltaron sin tocar. El bug de fondo en `SM_PaymentHandler.cls` (DML no bulkificado + la inconsistencia Canceled/Completed) **no se corrigió en el código** -- solo se hizo el backfill de datos | **Backfill aplicado y verificado** (11/11 pagos, 1/1 orden, 0 errores). **Pendiente real**: arreglar `SM_PaymentHandler.cls` (usar `Database.update(list, false)` + separar el caso ACCEPTED del REJECTED para que mande a `Completed` en vez de `Canceled`) para que esto no se repita con el próximo contrato Cancelled que tenga un pago tardío |
| 2026-09-15 | `ACH_REPORTADOS/build_index.py` (índice de transmisión ACH) no tenía ningún corte de antigüedad -- a diferencia del `build_index.py` de Returns/Collection (corte de 7 días desde 2026-09-09, sección 2.2 de `CLAUDE.md`), cualquier archivo nunca antes visto se aplicaba solo a Salesforce sin importar la fecha que trajera su nombre. Además, un archivo que daba 0 registros (vacío/feriado) o sin fecha reconocible en el nombre nunca quedaba marcado como "visto" (no escribía fila en `transmission_raw_log.csv`), así que se re-escaneaba y reportaba en consola en TODAS las corridas futuras, para siempre | Se detectó porque `run_daily_new_files.sh apply` reportó `ACH_20240619.csv` (jun-2024) y `ACH_20260311.csv` (mar-2026) en la corrida diaria del 2026-09-15 -- ambos resultaron ser archivos vacíos (16 y 0 bytes) sin impacto real, pero confirmó el hueco: si hubieran tenido registros reales, se habrían aplicado a Salesforce automáticamente sin ningún aviso ni corte de antigüedad. Al correr el fix se encontraron 19 archivos en total con este mismo problema de re-escaneo (todos vacíos) | `NEW_FILE_MAX_AGE_DAYS = 120` (~4 meses) + corte por año anterior, mismo patrón que Returns/Collection: el archivo se indexa igual (no se pierde del histórico) pero sus filas NO entran a `last_run_delta.csv` (lo que se aplica a Salesforce) -- quedan en `archivos_viejos_pendientes_confirmacion.csv` para aplicar manual. Nuevo `scanned_files_log.csv` registra todo archivo examinado (con o sin registros) para que nunca se vuelva a rescanear. Probado con una copia aislada de datos reales (archivo de 819 días con 30 pagos reales: quedó excluido del delta pero conservado en el índice) | **Desplegado localmente** (commit `0c96664`, sin push) -- verificado con corrida real (19 archivos vacíos dejaron de reportarse en la segunda corrida) |
| 2026-09-15 | Set completo de reportes nativos de Salesforce para Comercial, en `CollectionsFolder` (misma carpeta que ya usan) | Antes solo existía el correo diario de return codes — Comercial no tenía dónde ver el detalle en vivo directamente en Salesforce | 6 reportes con `reportType=Contract_and_Payment__c` (join `Contract`→`Payment_Data__r`): `R02_Cuenta_Cerrada`, `R10_Cliente_No_Autoriza`, `R04_R13_Cuenta_Routing_Invalido`, `R07_Autorizacion_Revocada`, `R16_Cuenta_Congelada` (filtran por `SM_Return_code__c`, en vivo), y `Contratos_Flagueados_Cobranza` (lista fija de 5 contratos, no se auto-actualiza — hay que editar el filtro a mano si se agrega uno nuevo) | **Desplegados y validados** — todos pasaron la validación de campos/join de Salesforce al deploy |
| 2026-09-14 | Backfill de 5 contratos ACH con AC completo (validado por Payments reales) nunca activados: `00317934, 00317935, 00317965, 00318211, 00318375` | Ingreso recurrente bloqueado — mismo patrón que los 11 ChargeBee/CC y los 3 ACH ya resueltos el 2026-09-12/13 | Activación directa (`SM_AC_collected__c=true`, `Status=Activated`, `StartDate=hoy`, `SM_Charged_AC_Amount__c`/`SM_Charged_AC_Fees__c`/`SM_Payment_Received__c` fijados manualmente) sin bypass — `CONTRACT_Create_ACH_Subscription_Order` creó la Subscription Order sola en los 5, sin duplicar la orden AC (fix del 2026-09-13 confirmado funcionando otra vez) | **Desplegado y validado** — 5/5 activados. Al validar apareció un doble cobro real en `00318211` (ver fila siguiente), no relacionado con el backfill en sí |
| 2026-09-14 | Contrato `00318211`: AC cobrado doble — confirmado por Carlos | `ACH-29121` ($99) es un segundo cobro real de AC, además de `ACH-28531`/`PY-01880305` que ya cubría el total | N/A — mecanismo de abono/refund pendiente de definir, igual que `00317786` | **Anotado, sin resolver** — mismo tipo de caso que `00317786` |
| 2026-09-14 | Backfill de 25 Late Payment Fee huérfanas (`SM_Failed_payment__r.Payment_Status__c='ACCEPTED'` pero orden no `Canceled`/`Completed`) — la mayoría (11) eran efecto colateral de los propios batches de hoy (Grupo B, reconciliación), que usaron `avoidAllHandlerExcecution=true` y por eso bloquearon sin querer también a `SM_LateFeeReconciliationHandler`; el resto eran casos históricos previos a la automatización | $2,833 en 25 órdenes sin cancelar | 24/25 canceladas con `Database.update(list, false)` + nota `AUTO:` (backfill manual). `ACH-28192` (00309045, $49) quedó **excluida a propósito** — tiene 3 pagos `ACH TRANSMITTED` en curso ($49 c/u, transmitidos 25-26 ago) sobre la misma orden que ya se había aceptado antes, más una nota manual previa de "REFUND POR AJUSTE" — huele a cobro duplicado real, la validation rule bloqueó correctamente la cancelación | **24/25 resuelto**. `ACH-28192` pendiente de revisión directa de Carlos — no forzado |
| 2026-09-14 | Extendido `SM_ReturnCodeNotifier` a **R04+R13 (cuenta/routing inválido), R07 (autorización revocada), R16 (cuenta congelada)** — revisión completa de los 24 códigos de retorno presentes en el org, descartando R01/R09 (NSF, se resuelve solo reintentando) y R08 (pago detenido, puede ser puntual, no todos los futuros — queda pendiente decidir) | **R04/R13: 24 contratos** (40 pagos) · **R07: 8 contratos** (13 pagos) · **R16: 2 contratos** (7 pagos) sin resolver | `sendReport()` generalizado a recibir `Set<String>` de códigos (permite combinar R04+R13 en un solo reporte); 3 scripts nuevos (`notify_invalid_account.apex`, `notify_r07.apex`, `notify_r16.apex`), conectados a `run_daily_new_files.sh` junto a R02/R10 (ahora 5 correos por corrida) | **Desplegado y validado en producción** — 8/8 tests, los 3 correos nuevos confirmados enviados |
| 2026-09-14 | Extendido `SM_ReturnCodeNotifier` a **R10 (CUSTOMER ADVISES NOT AUTHORIZED)** — el cliente le dijo al banco directamente que no autoriza el cobro | **13 contratos activos con R10 sin resolver, 17 pagos afectados** | Clase generalizada (`sendReport(code, label, explanation)`), `sendR02Report()`/`sendR10Report()` como wrappers finos; `COLLECTIONS/notify_r10.apex` nuevo, conectado a `run_daily_new_files.sh` junto al de R02 | **Desplegado y validado en producción** — 5/5 tests, corrida real confirmó 13 contratos/17 pagos y el correo llegó correctamente |
| 2026-09-14 | No existía ninguna alerta para ACH Return código **R02 (ACCOUNT CLOSED)** — a diferencia de NSF, significa que la cuenta bancaria del método de pago ya no existe, reintentar no sirve de nada. Se descubrió el patrón al validar el contrato `00316708` (6 meses seguidos sin cobrar, 3 de los rechazos R02) | **19 contratos activos con R02 sin resolver, 78 pagos afectados** — nadie lo había notado porque solo era visible investigando contrato por contrato | `SM_ReturnCodeNotifier.cls` (nuevo) + `COLLECTIONS/notify_r02.apex` — consulta en vivo todos los `SM_Payment__c` con `SM_Return_code__c='R02'` en contratos activos (excluye Cancelled/Finalized/Customer Cancellation), y marca si el contrato tuvo un pago `ACCEPTED` después del último R02 (posible ya resuelto). Enviado por correo a `clopez@legal-credit.com`. Conectado a `run_daily_new_files.sh` — se dispara solo en modo `apply` | **Desplegado y validado en producción** — 4/4 tests, corrida real confirmó 19 contratos/78 pagos y el correo llegó correctamente |
| 2026-09-14 | Validación en vivo de `SM_LateFeeReconciliationHandler` y la convención `AUTO:` — al actualizar 3 pagos AC (`PY-01887711`, `PY-01890512`, `PY-01890575`) sin bypass, la cadena completa corrió sola: activación de contrato, creación de Subscription Order, **y cancelación automática de 3 Late Payment Fee huérfanas** ($124, $144, $164) que nadie pidió tocar explícitamente | N/A — esto confirma que el fix del 2026-09-13/14 funciona en producción real, no solo en tests | N/A | **Confirmado en producción** — reduce en 3 el backlog de 24 LPF huérfanas detectado el 2026-09-13 |
| 2026-09-14 | Ninguna automatización dejaba rastro de sí misma — `LastModifiedById` de cualquier registro tocado por un trigger, Flow, job programado, o el `sf` CLI/Claude Code siempre queda con la identidad `clopez@legal-credit.com`, indistinguible de una acción manual de Carlos | Caso real que disparó el hallazgo: 2 `SM_ACH_Order__c` nuevas (`ACH-29452`/AC duplicada, `ACH-29453`/Subscription) aparecieron en el contrato `00317929` un domingo a las 5:57 AM hora Puerto Rico, sin ningún `CronTrigger`/`AsyncApexJob` que lo explicara — imposible determinar el origen sin auditoría externa | Convención nueva (ver `CLAUDE.md` sección 1): toda automatización que toque `SM_Payment__c`/`SM_ACH_Order__c`/`Contract` deja una marca `"AUTO: <proceso> - <acción> - <fecha>"` en `SM_Id_Salesforce_LCS__c`, preservando el valor anterior. Helper nuevo `SM_AutomationLogHelper` (Apex) + fórmula equivalente en Flows. Retrofit aplicado a `SM_ACPaymentActivationHandler`, `SM_LateFeeReconciliationHandler`, y los 3 flows de ACH (`PAYMENT_Accumulate_AC_On_Contract`, `CONTRACT_Create_ACH_AC_Order` v3, `CONTRACT_Create_ACH_Subscription_Order` v3) | **Desplegado y validado en producción** (deploys exitosos, tests 12/12 en la parte Apex; los 3 flows activados en su nueva versión). Pendiente: el origen real de `ACH-29452`/`ACH-29453` de esta mañana sigue sin explicación — no se pudo determinar con las herramientas disponibles |
| 2026-09-10 | Campo `SM_Chargent_Orders_Transaction__c` eliminado del org, pero todavía referenciado en `SM_FeePaymentToDependentContract.cls` y `SM_AcPaymentToDependentContract.cls` | Cualquier pago Fee/AC/Late Payment Fee que pasa a `ACCEPTED` en un contrato Master con dependientes tumba **todo el lote bulkificado** de la transacción (no solo el registro que lo dispara). Barrido del org: 1,764 pagos ($215,034.21) en 47 contratos Master en riesgo latente | Línea comentada en ambas clases (nota `// PARTE APP DE CHARGENT. ELIMINADA JUL 2026`), ajuste de tests (`SM_FeePaymentToDependentContractTest`), deploy con `RunSpecifiedTests` | **Desplegado y validado en producción** — `PY-01786030` aplicado con éxito post-fix, clonado a contrato dependiente confirmado (`PY-01900409`) |
| 2026-09-07 (encontrado) / 2026-09-10 (resuelto) | `update_check_collection.apex`: aplicar un pago sobre un contrato **Cancelado** cuyo `Payment_Status__c` cambia dispara un intento de cancelar su ACH Order; si esa orden tiene otro Payment `ACH TRANSMITTED`, una validation rule tumba **toda la oleada** del día (caso real: `ACH-27553`/00317537 tumbó 161 pagos de una corrida) | Bloqueaba la sincronización normal del backlog de Check Collection cada vez que aparecía un contrato Cancelado en el lote | Guard que detecta el caso ANTES del DML y lo separa para revisión manual/aislada (ya en el script); los 2 casos concretos (`PY-01876062`, `PY-01810250`) se procesaron uno por uno, aislados | **Resuelto para los 2 casos conocidos** — el guard en el script sigue vigente para cualquier caso futuro |
| 2026-09-10 | Informe ejecutivo general (Caso 01) afirmaba "37 de 39 ya cobraron con éxito" sin verificación en vivo — en realidad ninguno tenía confirmación bancaria, solo la orden interna marcada `Completed` (no equivale a cobro confirmado) | Cifra incorrecta a punto de presentarse a gerencia | Reverificación en vivo de los 39 uno por uno, corrección del texto del informe y el pill de estado (`Resuelto` → `En espera del banco`) | **Corregido en el informe**, publicado |
| 2026-09-10 | Contrato 00318139: única orden (AC, $99) en `Pending`, sin `SM_Next_Transaction_Date__c` — nunca se transmitió al banco, contrato detenido >1 mes | Cliente sin ningún intento de cobro, ni exitoso ni fallido | Se le asignó fecha de próxima transacción (15-ago) para que el batch diario lo recoja | **En cola** — pendiente confirmar que el batch efectivamente lo transmita |
| 2026-09-10 | Backlog de 50 pagos ($4,331.50, 29 contratos) con reporte del banco ya recibido y guardado en `index/collections_index.csv`, pero nunca sincronizado a Salesforce (retenido por la Regla 2 — >2 meses sin confirmación explícita) | Dinero ya cobrado por el banco, sin reflejar en Salesforce | Confirmación explícita de Carlos + CSV scoped a esos 50 + `update_check_collection.apex`/`update_check_collection_isolated.apex` | **50/50 aplicados** (incluye el que estaba bloqueado por el bug de Chargent, arriba) |
| 2026-08-14 (evento) / 2026-09-09 (encontrado y corregido) | Update masivo marcó 12 órdenes AC como `Completed` y les borró la fecha de próximo cobro, sin haber generado nunca el pago — el batch diario las ignoraba por verlas "ya resueltas" | 12 contratos activos congelados indefinidamente — nunca iban a cobrar su AC ni activar su suscripción | Revertidas a `Initiated` con su fecha de AC original | **Resuelto** — las 12 ya cobraron con éxito, $1,348 recuperados |
| 2026-08-14 (evento) / 2026-09-09 (encontrado) | Proceso masivo `ADJ_AC_ERR_12AGO` generó 90 órdenes de AC con montos erróneos (mezcló valor de AC con mensualidad) | 61 contratos ya cobraron de más ($4,382.47 total), 1 cobró de menos ($74) | Analizado a fondo (`ADJ_AC_ERR_12AGO_Analisis_Pagos_61.csv`), mecanismo de reembolso sin decidir todavía | **Analizado, pendiente decisión de negocio** (Caso 06 del informe general) |
| ~2026-08 (evento) / 2026-09-08/09 (encontrado) | El paso automático que reactiva una Late Payment Fee tras confirmación de rechazo del banco falla intermitentemente — la multa se queda `Stopped` para siempre sin que nadie lo note | 88 contratos con cuotas legítimamente vencidas (algunas desde abril) nunca se reintentaron | 100 órdenes reactivadas manualmente (97 vía script + 3 por Gerencia) | **Resuelto** — 96 de 97 cobraron con éxito, ≈$12,046 recuperados |
| ~2026-08 (root cause) / 2026-09-06 (encontrado) | Al retirar la app Chargent se deshabilitó por error el trigger `SM_ChargentTransactionTGR` (mal nombrado — sin relación real con Chargent), la única pieza que sumaba el pago de AC al contrato | 24 contratos con AC ya pagado (julio) nunca se activaron ni empezaron a facturar suscripción — ingreso recurrente bloqueado semanas | Flow permanente `PAYMENT_Accumulate_AC_On_Contract` desplegado 2026-09-02, reemplaza la pieza rota; los 24 contratos activados manualmente | **Resuelto** — sin contratos huérfanos nuevos desde el despliegue |
| 2026-09-06 | `SM_ContractHandler` en MONEE (producción) es una **cáscara vacía** (12 métodos no-op) desde 2026-07-09 — la lógica real completa solo existe en PREPROD; `SM_ContractHandlerTest` en MONEE es la versión 2022 (pre-Chargent), no asigna Banco a Payment Methods ACH → falla PM003 | Toda la lógica del trigger de Contract (activación de assets, status maestro/dependiente) corre como no-op en producción; además bloquea cualquier deploy que dispare `RunLocalTests` | Carlos está sincronizando manualmente MONEE/PREPROD/repo — **NO TOCAR sin confirmar con él primero** (ver `CLAUDE.md` sección 3) | **Abierto, trabajo en curso del propio Carlos** |
| 2026-09-12 | Ningún mecanismo activaba un contrato de ChargeBee/Credit Card cuando su AC llegaba `ACCEPTED` — `SM_paymentTGR_Handler` (la única pieza que sabe poner `SM_AC_collected__c=true`/`Status=Activated`) exige `SM_ACH_Order__c` (los pagos de ChargeBee nunca lo tienen) y además está con los 6 flags de `SM_Trigger_Panel__mdt` en `false`; el reemplazo legado `SM_chargentOrTr_Handler` solo se disparaba desde `ChargentOrders__Transaction__c`, objeto que ChargeBee nunca crea. El flow hermano `PAYMENT_Accumulate_AC_On_Contract` (desplegado 2026-09-02 para la fila de arriba, root cause `SM_ChargentTransactionTGR`) tampoco cubre este caso: dispara solo en `Update` de `Payment_Status__c` (ChargeBee crea el pago ya `ACCEPTED`, nunca dispara un `Update`) y lee `SM_ACH_Order__r.SM_Total__c` (blanco sin ACH Order) | 26 contratos ChargeBee reales encontrados en `Payment Process` con AC ya `ACCEPTED` pero nunca activados (ej. `00318338`, `SM_Charged_AC_Amount__c` seguía en 0) — ingreso recurrente de esos contratos nunca arrancó | Trigger + handler nuevos y aislados (`SM_ACPaymentActivationTrigger`/`SM_ACPaymentActivationHandler`, con su propio `SM_Trigger_Panel__mdt`) que activan el contrato cuando un pago sin `SM_ACH_Order__c` y `SM_Type__c='AC'` llega a `ACCEPTED` — mismo cálculo de acumulación que ya usan los handlers de ACH/Chargent, soporta AC partido en 2 pagos. Deliberadamente separado de `SM_PaymentHandler`/`SM_paymentTGR_Handler` (en sync manual de Chargent, ver fila de `SM_ContractHandler`) | **Desplegado y validado en producción** (`sf project deploy start`, `RunSpecifiedTests`, 13/13 tests, 0 fallos). Backfill del histórico corrido 2026-09-12: de los 26 candidatos iniciales, solo **11 tenían un pago AC realmente `ACCEPTED`** (los otros 15 solo tenían Fee/Late payment fee aceptados, correctamente sin tocar) — los 11 se activaron: `00318128, 00318277, 00318338, 00318362, 00318366, 00318389, 00318395, 00318426, 00318444, 00318497, 00318537` |
| 2026-09-12 | El flow `PAYMENT_Accumulate_AC_On_Contract` (fila de arriba) solo acumulaba `SM_Charged_AC_Amount__c`/`SM_Charged_AC_Fees__c` — nunca comparaba contra `SM_Total_AC__c` ni ponía `SM_AC_collected__c=true`/`Status=Activated`. Mismo síntoma que la fila de ChargeBee, pero del lado ACH | Confirmado con 2 contratos reales (`00318054`, `00317976`) con el AC ya acumulado por encima del total pero nunca activados — sin activación, tampoco disparaban `CONTRACT_10_After_Save_Orchestrator` → nunca se creaba el Subscription Order | Nueva decisión `If_AC_Total_Reached` + record-update `Activate_Contract_AC_Complete` agregados al flow (nueva versión 2, activada vía `FlowDefinition.activeVersionNumber`); también se agregó guard `SM_AC_collected__c=false` a la elegibilidad para evitar reprocesar un contrato ya activado | **Desplegado y validado en producción** — los 2 contratos backfillados exitosamente tras el fix de abajo |
| 2026-09-12 | `CONTRACT_Create_ACH_Subscription_Order` nunca llenaba `SM_Next_Transaction_Date__c` al crear la orden Subscription; `SM_ACHOrderHandler.insertNextScheduledPaymentDate` (línea 588) hace `.year()` sobre ese campo sin validar null | Bloqueaba en vivo la activación de `00318054`/`00317976` (`NullPointerException`, `CANNOT_INSERT_UPDATE_ACTIVATE_ENTITY` en `SM_ACHOrderTrigger` BeforeInsert) — de hecho bloqueaba la creación de CUALQUIER Subscription Order de ACH vía este flow, para cualquier contrato | Ya existía una **versión 2 de este mismo flow, en Draft desde el 26-ago-2026, que ya traía el fix** (`SM_Next_Transaction_Date__c = sf_Contract.SM_Start_date__c`) — nunca se había activado. Se activó vía `FlowDefinition.activeVersionNumber=2`, sin tocar el contenido | **Desplegado y validado en producción** — confirmado con las 2 Subscription Orders creadas correctamente (`ACH-29450`, `ACH-29451`), sin duplicados |
| 2026-09-13 | `CONTRACT_Create_ACH_AC_Order` (dispara en cada update de `Status` vía `CONTRACT_10`, no solo al crear) busca si ya existe una orden AC con `SM_Payment_Status__c NotEqualTo 'Completed' AND NotEqualTo 'Canceled'` — para una orden `Once` ya completada, nunca la encuentra y crea **una AC duplicada** cada vez que el contrato se actualiza después de tener su AC ya cobrado | Al activar manualmente 3 contratos ACH (`00317976`, `00318054`, `00317929`, backfill AC-ya-pagado) se les creó una orden AC duplicada `Pending` con fecha de transacción vencida (`ACH-29448/29449/29452`) — el batch diario de cobro (corre Lun-Vie) las habría recogido e intentado cobrar el AC **por segunda vez** a esos clientes. Confirmado que NO es un problema histórico: 5 contratos activados normalmente en agosto solo tienen 1 orden AC cada uno — el bug solo se manifiesta cuando `Status` cambia a Activated con la orden AC ya en `Completed` en la MISMA transacción del update, algo que la ruta normal (histórica) nunca provocó pero mi backfill manual sí | `Get_AC_ACH_Order` ajustado para buscar cualquier orden AC del contrato sin filtrar por status (igual regla que pidió el usuario: si ya existe una orden AC, no crear otra, sin importar su estado) | **Desplegado y validado en producción** (búsqueda confirmada contra un contrato real) — **pendiente, a decisión del usuario**: las 3 órdenes AC duplicadas ya creadas (`ACH-29448`, `ACH-29449`, `ACH-29452`) siguen `Pending` con fecha vencida, no se cancelaron todavía |
| 2026-09-13 | Backlog de `SM_Payment__c` con `SM_Check_Collection_Status__c = 'PENDING'` desincronizado del veredicto final real del banco — 1,561 pagos en ese estado org-wide; de los 839 de los últimos 6 meses, **29 (3.5%) ya tenían un veredicto final** (28 `COLLECTED`, 1 `NOT_COLLECTED`) en los índices `RETURNS/COLLECTIONS` que nunca se sincronizó a Salesforce. El 96.5% restante (810) sigue genuinamente `PENDING` — consistente con la regla de negocio, no es backlog | Dinero ya cobrado por el banco (28 casos) sin reflejar en Salesforce; 1 caso con rechazo final tampoco reflejado. Validado además contrato-por-contrato con conteo real de Payments (no con campos fórmula) si "al día" al aplicar el cambio — reveló pagos duplicados (`00317786`, AC cobrado 2x) y splits parciales (`00317534`, solo mitad cobrada) que NO deben tratarse igual | Cambios aplicados con `SM_TriggerHandler.avoidAllHandlerExcecution = true` (patrón ya usado en `scripts/apex/Crear_Payments_Manual_SM_PaymentBatch.apex`) para no generar Invoice/recibo/clonado a dependientes al hacer el update masivo. Grupo A (contrato `00314090`, 5 pagos + 5 órdenes "Late payment fee"/Once) llevado a `ACCEPTED`/`COLLECTED` + orden a `Completed`. 6 pagos PENDING adicionales del mismo contrato validados aparte: 4 `NOT_COLLECTED`, 1 `COLLECTED`, 1 genuinamente sigue `PENDING` (`PY-01760901`, sin tocar) | **Parcial** — Grupo A y el extra de `00314090` aplicados y verificados. Grupo B (21 órdenes Subscription/Monthly, 22 contratos — 8 Cancelled, 14 Activated/Payment Process) **NO se debe marcar la orden como `Completed`** (significaría "ya no cobrar más" en una suscripción activa) — solo listado, sin tocar, pendiente decisión de Carlos. 1 pago tipo AC (`PY-01812404`/`00316911`) excluido a pedido explícito, pendiente que Carlos lo revise aparte |
| 2026-09-13 | `collections_index.csv` (histórico bulk-cargado) no está en orden cronológico real — se cargó agrupado por bloques de mes en orden **alfabético** de nombre de archivo (April, Feb, Marh...) y, dentro de cada bloque, por orden alfabético del nombre completo del PDF (`"April 1"` < `"April 10"` < `"April 13-14"` ... < `"April 2-3"`, orden de texto, no de fecha). La regla "la fila que aparece después en el archivo es la más reciente" (correcta para la carga diaria incremental, documentada en `build_pending_deltas.py`) es **falsa** para este bloque histórico | Cualquier cruce que determinara "cuál es el último veredicto del banco" usando orden de aparición en el CSV en vez de la fecha real del nombre del archivo podía elegir el reporte equivocado. Además, 224 pagos org-wide tienen **filas contradictorias dentro del MISMO archivo** (reportes de rango multi-día, ej. "April 2-3", sin separar qué fila es de qué día) — ambigüedad real, no resoluble sin un reporte posterior que desempate | Reconstruido el cruce usando un parser de fecha real del nombre de archivo (no el orden de fila) para los 22 contratos del "Grupo B"; verificado que los cambios YA aplicados (Grupo A de `00314090`, `PY-01812404`) seguían siendo correctos bajo la lógica corregida — no requirieron rollback. Al auditar más a fondo esos mismos 22 contratos aparecieron **15 pagos desincronizados adicionales** que el cruce original de 839 pagos nunca había detectado, más 1 pago con conflicto interno real (`PY-01812395`, sin reporte posterior que lo resuelva) y 1 pago con estado `RETURN` (no `PENDING`) que el filtro original tampoco cubría (`PY-01872955`, contrato `00316490`) | **Parcial** — el contrato `00316490` completo ya se resolvió (ver fila de abajo). El resto de los 35 candidatos limpios del "Grupo B ampliado" (21 originales + 14 nuevos) sigue pendiente de confirmación. **Recomendado pendiente**: re-auditar el universo completo de 839 pagos con la lógica de fecha corregida — el alcance real del backlog podría ser mayor a lo estimado originalmente |
| 2026-09-13 | Grupo B ampliado (21 contratos, 35 pagos tipo `Fee`, orden ACH Subscription/Monthly) — confirmados `COLLECTED` por el banco con la lógica de fecha corregida, `$2,860` total, 6 contratos con 2-3 pagos consecutivos cada uno | 7 de los 21 contratos `Cancelled` (`00313871, 00315355, 00315765, 00315786, 00315999, 00316074, 00316695`), 14 `Activated` | Payments actualizados a `ACCEPTED`/`COLLECTED` con `avoidAllHandlerExcecution=true`; **las órdenes ACH NO se tocaron** (Subscription/Monthly, marcarlas `Completed` sería incorrecto) | **Resuelto y verificado en producción** — 35/35 confirmados `ACCEPTED`/`COLLECTED` |
| 2026-09-13 | Contrato `00316490` — 3 pagos con veredicto final confirmado por Carlos directamente: `PY-01872955` (RETURN→`NOT_COLLECTED`, NSF, sin más reintentos), `PY-01812395` (Fee/Monthly, conflicto interno en el índice→`COLLECTED`, confirmado por Carlos), `PY-01786086` (AC/Once→`COLLECTED`, confirmado por Carlos) | Contrato `Cancelled`, `SM_AC_collected__c` ya en `true` desde antes | Payments actualizados con `avoidAllHandlerExcecution=true`; la orden ACH de `PY-01786086` (AC/Once, antes `Canceled`) se pasó a `Completed` siguiendo el mismo criterio que Grupo A (orden `Once` refleja el cobro real); la orden de `PY-01812395` (Subscription/Monthly) se dejó `Canceled` sin tocar | **Resuelto y verificado en producción** |
| 2026-09-12 | `SM_PaymentHandler.updateRelatedRecordsByPaymentStatusUpdates` (línea 258) dereferenciaba `achOrder` sin validar null — cualquier pago sin `SM_ACH_Order__c` (Credit Card/ChargeBee/Cash) cuyo `Payment_Status__c` cambiara a `ACCEPTED`/`REJECTED` vía **update** (no insert) tumbaba `NullPointerException` y revertía toda la transacción | Confirmado en vivo (simulación de solo lectura + reproducido real en el test runner de un deploy) — bloqueaba silenciosamente cualquier corrección manual o integración que actualizara el status de un pago no-ACH ya existente | Guard `if (achOrder == null) { continue; }` agregado antes del `switch`. De paso se sincronizó el archivo local (estaba desactualizado frente a MONEE — todavía llamaba a `processRejectedPaymentsChargentOrder` con un guard `if(canAccessChargent)` que MONEE ya había limpiado) | **Desplegado y validado en producción** — cubierto por `SM_PaymentHandlerNonAchStatusTest.cls` (nuevo, 2 tests) + `SM_PaymentHandlerTest` existente (6/6 sin regresión) |
| 2026-09-14 | Adobe Sign (`echosign_dev1__SIGN_Agreement__c`) — reenviar un Draft atascado vía API (`echosign_dev1__Background_Action__c='Send'`) tiene 3 trampas encontradas en vivo: (1) `sf data update bulk` (Bulk API v2) con un valor vacío en el CSV **no** limpia el campo a `null` — lo deja igual, así que re-poner `'Send'` después no dispara nada porque el paquete nunca ve el cambio de valor; hay que usar `sf data update record` (REST) para el reset. (2) Actualizar muchos registros en una sola transacción hace que el paquete encole **un solo** `future` (`EchoSignApiUtilities.sendDocumentAsync`) que intenta cargar todos los documentos en memoria a la vez → `Apex heap size too large` (probado con 24, exception, 0 enviados). Hay que hacerlo de a uno. (3) El campo `echosign_dev1__Sender_User__c` ("Send On Behalf Of") y el setting org-wide `echosign_dev1__Allow_Delegated_Sending__c` ("Allow Sending On Behalf of Others", en `echosign_dev1__SIGN_Settings__c`) **no tienen efecto** en esta ruta de recuperación — el remitente real (`echosign_dev1__Sender_Recorded__c`) siempre queda como el usuario que ejecuta la acción, sin importar esos dos campos. La delegación de remitente solo aplica al flujo normal de envío (`AgreementTemplateProcessor.autoSend`, el botón "Send for Signature" del Contract) | La causa raíz real de 44+ Drafts nunca enviados en agosto-septiembre 2026 era la cuenta de Adobe Sign sin transacciones disponibles (ajena a este repo, resuelta por Carlos con Adobe). Al intentar reenviar 24 Drafts recuperados en un solo bulk update, **ninguno se envió** (heap exception, 0/24). Reenviando de a uno vía REST, si funcionó — pero de los 2 que se probaron en vivo (con clientes reales), **ambos salieron con Carlos (`clopez`) como remitente en Adobe Sign**, no con el vendedor Owner real — correo real ya enviado, no reversible | Para reenvíos puntuales sin importar atribución: reset individual vía REST + `Background_Action__c='Send'` uno por uno (nunca en bulk). Para que quede atribuido al vendedor correcto: no usar este mecanismo — usar Login-As por vendedor + botón nativo "Send for Signature" en el Contract | **Documentado, sin fix de código posible** (comportamiento del paquete gestionado). 2 de 24 Drafts se reenviaron así (atribución incorrecta aceptada); 22 quedaron pendientes de reenvío vía Login-As |

---

## Reglas de negocio aprendidas en el camino (para no repetir el mismo error de análisis)

Estas no son bugs — son interpretaciones correctas de estados que a simple vista parecen rotos
pero no lo están. Ver también memoria local de Claude Code (`feedback_*.md`) para el detalle
completo de cada una.

- **`SM_Check_Collection_Status__c = PENDING` o `RETURN`** = el banco todavía está
  procesando/reintentando por su cuenta — no es un rechazo final, no se puede tocar hasta que
  llegue un veredicto (`ACCEPTED`/`COLLECTED` o `NOT_COLLECTED`).
- **Orden `Completed` + Payment en `ACH TRANSMITTED`** = normal, se marcará `ACCEPTED` en cuanto
  llegue el reporte, mientras no venga rechazado.
- **Orden `Canceled` + cero Payments** = puede ser legítimo (se canceló antes de la corrida diaria
  de creación de Payments a las 2:50, no aplicaba para cobro, o error/duplicidad) — no asumir bug.
- **`SM_Id_Salesforce_LCS__c` poblado + `SM_Is_Migrated__c = true`** = el registro llegó así desde
  una migración de datos, no es una brecha operativa del pipeline en vivo.
